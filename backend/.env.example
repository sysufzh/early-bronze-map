# 数据库连接
BRONZE_DB_HOST=localhost
BRONZE_DB_PORT=5432
BRONZE_DB_NAME=early_bronze
BRONZE_DB_USER=bronze_user
BRONZE_DB_PASSWORD=change-me

# JWT 密钥（openssl rand -hex 32 生成）
BRONZE_JWT_SECRET=change-me-to-random-hex
